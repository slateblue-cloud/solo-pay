{
  "name": "@solo-pay/faucet-manager",
  "version": "0.1.0",
  "description": "Gas faucet logic: one-time native token grant per wallet per chain for approve gas",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/server.js",
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:coverage": "vitest --run --coverage",
    "lint": "eslint",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@fastify/swagger": "^9.6.1",
    "@fastify/swagger-ui": "^5.2.4",
    "@solo-pay/database": "workspace:*",
    "dotenv": "^16.4.0",
    "fastify": "^5.0.0",
    "viem": "^2.44.4",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^4.0.17",
    "typescript": "^5.4.0",
    "vitest": "^4.0.14"
  },
  "keywords": [
    "faucet",
    "gas",
    "payment",
    "blockchain"
  ],
  "author": "MSQ Team",
  "license": "MIT"
}
