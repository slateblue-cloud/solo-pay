{
  "name": "@solo-pay/integration-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Integration tests for SoloPay monorepo",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "test:e2e": "playwright test -c e2e/playwright.config.ts",
    "test:e2e:headed": "playwright test -c e2e/playwright.config.ts --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@solo-pay/contracts": "workspace:*",
    "@solo-pay/gateway-sdk": "workspace:*",
    "@types/node": "^20.0.0",
    "dotenv": "^16.4.0",
    "ethers": "^6.13.0",
    "typescript": "^5.4.0",
    "vitest": "^4.0.14"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
