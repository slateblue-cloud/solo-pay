# 2026-02-15 Solo-Pay Docker + E2E 세션

## Docker 완전 해결

- **근본 원인**: Prisma 7.4.0의 기본 engineType이 `client`로 변경됨 → adapter 필수
- **해결**: `@prisma/adapter-mariadb` + `PrismaMariaDb` 패턴으로 `client.ts` 전환
- `.npmrc`에 `public-hoist-pattern[]=@prisma/*` 추가 (pnpm deploy에서 @prisma/\* hoist)
- `docker/copy-prisma.sh` 스크립트로 `.prisma/client` deploy 대상에 복사
- custom output (`generated/client`) 제거 → 기본 `@prisma/client` 위치 사용
- **11/12 서비스 healthy** (price-service만 CMC_API_KEY 미설정)

## sample-merchant Prisma 7 업그레이드

- 서브에이전트가 6.19.2 → 7.4.0 업그레이드 완료
- `app/lib/prisma.ts`, `prisma/seed.ts`에 adapter 패턴 적용
- **SSR 500 에러 발생** — Next.js SSR에서 Prisma adapter import 문제. 미해결.

## 코드 리뷰 (CODE_REVIEW.md)

- High 3건: CORS all origins, rate limiting 없음, webhook 서명 검증 없음
- Medium 12건: 중복 코드, N+1 쿼리, dead code 등
- `any` 타입 0건 (깨끗)

## E2E Playwright 테스트

- `packages/e2e-tests/` 생성
- **permit 결제 E2E 통과**: Gateway API → EIP-2612 permit 서명 → pay() → on-chain 검증
- API 경로: `/api/v1/payment` (prefix `API_V1_BASE_PATH = '/api/v1'`)
- 인증: `x-public-key: pk_test_demo` + `Origin: http://localhost:3000`
- wallet connect UI 테스트는 RainbowKit/wagmi 상태 관리 복잡 → Synpress 필요

## 전체 체크 결과

- lint: 0 errors, 0 warnings
- format: prettier 통과
- typecheck: 통과 (integration-tests `relayRequestId` → `status` 수정)
- unit test: 622/622 통과
- integration test: 67/67 통과 (pay() permit 파라미터 추가)
- e2e test: permit 결제 + health check 통과

## PR 상태 분석

- **immich #26228**: `llm-generated` 라벨, 닫힘. 이 레포 기여 불가
- **dify #32300**: mandu001이 일본어 IME 테스트 통과 확인. CI workflow 승인 대기
- **schematic-symbols #386**: "this is fixed" CHANGES_REQUESTED → 이미 main에서 수정됐을 가능성
- **opik #5083** ($150): 10일 무응답

## 아직 안 한 것

- 커밋/push (Steven 허락 대기)
- High 보안 이슈 수정
- sample-merchant SSR 에러 수정
